name: CI - Lista de Tareas

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  check-project:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Verificar que los archivos existen
        run: |
          test -f index.html
          test -f style.css
          test -f app.js
